struct ll
	value int
	next ll
end
fn int main:
	f:string = "hello world"
	f = "over"
	list:ll = new ll
	list.value = 10
	list.value = list.value*2
	return list.value
end


;fn int fib n int:
;	cond:bool = n > 1
;	if cond goto done
;	return 1
;	label done
;	n = n-1
;	x:int = fib(n)
;	n = n-1 
;	y:int = fib(n)
;	out:int = x+y;
;	return out
;end
