struct test {a:int b:int c:int};

let test0:test = {0,0,0};
fn i_love_toast(a:int)->int{
   return 8+23*a;
}
fn main()->int{
   let b:[8]int = {0,0,0,0,0,0,0,0};
   let i:int = 0;
   while(i<8){
      b[i] = i_love_toast(i);
      i = i+1;
   }
   return b[0];
}

 
